# Document Assistant Environment Variables
# Copy this file to .env and fill in your API keys

# =============================================================================
# API Provider Configuration
# =============================================================================
# Choose which API to use: 'openai' or 'vocareum'
# - 'openai': Use OpenAI API directly (requires valid OpenAI API key)
# - 'vocareum': Use Vocareum endpoint (for course environments)
API_PROVIDER=vocareum

# OpenAI API Key
# - For API_PROVIDER=openai: Use your personal OpenAI API key from https://platform.openai.com/api-keys
# - For API_PROVIDER=vocareum: Use the API key provided by your course/Vocareum
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Base URL (only used when API_PROVIDER=vocareum)
# Default Vocareum endpoint: https://openai.vocareum.com/v1
# Leave empty or comment out when using API_PROVIDER=openai
OPENAI_BASE_URL=https://openai.vocareum.com/v1

# =============================================================================
# Model Configuration
# =============================================================================
# Default model to use
DEFAULT_MODEL=gpt-4

# Temperature for responses (0 = deterministic, 1 = creative)
DEFAULT_TEMPERATURE=0
